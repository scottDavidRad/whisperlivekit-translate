# Local speech server. Tested with Python 3.12.
whisperlivekit==0.2.19
faster-whisper==1.2.1
ctranslate2==4.8.2
# Local diarization (Sortformer) and optional remembered voices (TitaNet).
# Both models use this existing NeMo dependency; no cloud voice service.
nemo_toolkit[asr]==3.0.0
httpx==0.28.1
mlx-whisper==0.4.3; sys_platform == "darwin" and platform_machine == "arm64"
