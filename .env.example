# Optional default server URL; can also be entered in the app Settings.
# Use your computer LAN address when opening the app on a phone.
# Use wss:// when serving the app over HTTPS. No API key is required.
VITE_WHISPERLIVEKIT_URL=
